#include "Particle.h"
#include "Vector.h"
#include "Point3D.h"
#include "ParticleSystem.h"
#include "SpatialGrid.h"
#include <iostream>
using namespace std;
int main() {
  cout << "//===========================================//" << endl
       << "// Fluid Simulation Project " << endl
       << "//===========================================//" << endl;

  // Testing print/debug statement
  /*Particle particle = Particle(1, 2, 3, Vector(4, 5, 6), Vector(7, 8, 9));*/
  /*cout << particle;*/

  /*Particle p = Particle(1, 1, 1, 1, Vector(0,0,0), Point3D(0,0,30));
  SpatialGrid grid = SpatialGrid(6, 10.5);
  grid.addParticle(p);
  int i;
  cin>>i;*/

}
